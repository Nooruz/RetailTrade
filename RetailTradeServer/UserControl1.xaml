<UserControl   x:Class="SalePageServer.UserControl1"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
               mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>

        <Style x:Key="TabItemStyle" TargetType="{x:Type dx:DXTabItem}">
            <Setter Property="Header" Value="{Binding Header}"/>
            <Setter Property="Content" Value="{Binding Content}"/>
            <Setter Property="AllowHide" Value="{Binding AllowHide}"/>
        </Style>
        
    </UserControl.Resources>
    
    <Grid>
        <dx:DXTabControl ItemsSource="{Binding TabDataItems}"
                         ItemContainerStyle="{StaticResource TabItemStyle}">
            <dx:DXTabControl.View>
                <dx:TabControlStretchView HideButtonShowMode="InAllTabs"
                                          SingleTabItemHideMode="HideAndShowNewItem"/>
            </dx:DXTabControl.View>
        </dx:DXTabControl>
    </Grid>
</UserControl>
